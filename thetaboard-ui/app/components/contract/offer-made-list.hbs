<div class="card card-user">
    <div class="card-header ">
        <div class="row">
            <div class="col-sm-12 text-left">
                <p class="card-text"></p>
                <div class="author">
                <div class="block block-one"></div>
                <div class="block block-two"></div>
                <div class="block block-three"></div>
                <div class="block block-four"></div>
                <h2 class="card-title domain-title">Offer Submitted</h2>
                </div>
            </div>
        </div>
    </div>
    <div class="card-body card-offers">
        {{#if this.offer.isConnectedWallet}}
            {{#each this.offer.offersMade as |offerMade|}}
                <div class="offer-made-cards made-offer-div">
                    <div>You made an offer to buy the Domain:</div>
                    <h3>{{offerMade.domainName}}</h3>
                    <div class="alert-success alert emphesized">
                        <span>Offer amount:</span>
                        <p>{{offerMade.offer.offerAmount}} Tfuel</p>
                    </div>
                    <br/><br/>
                    <div class="buttons-container">
                        <button {{on 'click' (fn this.offer.cancelOffer offerMade)}} type="submit" class="btn btn-fill btn-primary">Cancel Offer</button>
                    </div>
                </div>
            {{else}}
                <p class="offer-placeholder">No offer submitted.</p>
            {{/each}}
                <button type="submit" {{on 'click' this.connectWallet}} class="btn btn-fill btn-primary connect-wallet-offer-button refresh-offer">Refresh</button>
        {{else}}
            <p class="offer-placeholder connection-offer">Please <a href="" class="connect-wallet-offer-button" {{on 'click' this.connectWallet}}>connect your wallet</a> to access your offers.</p>
        {{/if}}
    </div> 
</div>